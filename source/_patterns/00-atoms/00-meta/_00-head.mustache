<!DOCTYPE html>
<html class="{{ htmlClass }}">
	<head>
		<title>{{ title }}</title>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width" />

        <!-- Option if CSS is being compiles within the styleguide -->
		<link rel="stylesheet" href="../../css/style.css?{{ cacheBuster }}" media="all" />

        <!-- Optional import of Drupal CSS files. -->
        {{# drupalStyleSheets }}
            {{# . }}
                <link rel="stylesheet" href="{{ drupalRoot }}/{{ . }}?{{ cacheBuster }}" media="all" />
            {{/ . }}
        {{/ drupalStyleSheets }}

		<!-- Begin Pattern Lab (Required for Pattern Lab to run properly) -->
		{% pattern-lab-head %}
		<!-- End Pattern Lab -->
	
	</head>
	<body class="{{ bodyClass }}">	
		